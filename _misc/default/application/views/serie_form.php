<?= $form->RenderItem('name');?>
<div class="help">
    <strong>Jak nastavit formát číselné řady ?</strong>
    <p>Zde je text, který popisuje jak lze nastavit formát číselné řady. Jsou zde vysvětleny jednotlivé zástupné symboly, které překládá
    jádro. Následuje popis dalších zástupných symbolů, které mohou překládat jednotlivé modely.</p>
</div>
<?= $form->RenderItem('format');?>